# Receipt App — Claude Code ガイド

## プロジェクト概要

レシート管理アプリケーション。

## Git 運用ルール

### 必須: 変更のたびに GitHub へプッシュ

コードを変更したら、毎回以下の手順を実行すること。

1. 変更をステージング
   ```
   git add <変更ファイル>
   ```
2. コミット（日本語メッセージ可）
   ```
   git commit -m "変更内容の説明"
   ```
3. GitHub へプッシュ
   ```
   git push origin <ブランチ名>
   ```

> **重要**: 作業が一段落したタイミングではなく、コードを変更するたびに push すること。ローカルにコミットを溜めない。

### ブランチ戦略

- `main` — 本番相当の安定ブランチ。直接プッシュ禁止。
- `develop` — 開発統合ブランチ。
- `feature/<機能名>` — 機能開発用。完了後は PR を作成して `develop` にマージ。

### コミットメッセージ規則

```
<種別>: <変更の概要>

種別:
  feat     新機能
  fix      バグ修正
  refactor リファクタリング
  style    フォーマット変更（動作に影響なし）
  test     テスト追加・修正
  docs     ドキュメント変更
  chore    ビルド・依存関係など
```

### PR ルール

- `main` へのマージは必ず PR 経由。
- PR 作成時は `gh pr create` を使用。
- マージ前にセルフレビューを行う。

## コーディング規則

- コメントは原則不要。必要な場合は「なぜ」を記述（何をしているかは書かない）。
- セキュリティ: ユーザー入力・外部 API のみバリデーション。内部コードは信頼する。
- 不要な抽象化・将来のための設計はしない。

## 開発コマンド

（セットアップ後に追記）
